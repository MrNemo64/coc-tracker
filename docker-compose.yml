services:
  coctrackertest:
    image: postgres:16-alpine
    ports:
      - 5432:5432
    volumes:
      - ./storage:/var/lib/postgresql/data
    environment:
      - POSTGRES_PASSWORD=test
      - POSTGRES_USER=test
      - POSTGRES_DB=test